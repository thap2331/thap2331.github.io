# Professional Portfolio

## First time set up build
- `sudo apt install rbenv`
- `gem install bundler`
- `bundle install`
- `bundle exec jekyll serve`
- Run `localhost:4000`


## Spin it up
- `bundle exec jekyll serve`
- Run `localhost:4000`


## Reser
- Reinstall Jekyll:
    - Uninstall and reinstall Jekyll to ensure it’s using the correct Ruby version:
    ```
        gem uninstall jekyll
       gem install jekyll
    ```